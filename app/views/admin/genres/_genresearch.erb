<table class="table table-hover table-inverse table-bordered" style="width: 80%">
  <thead>
    <tr><th class="text-center">ジャンル検索</th></tr>
  </thead>
  <tbody>
    <% @genres.each do |genre| %>
      <tr></tr><td><%= link_to genre.item_genre, items_path(genre_id: genre.id) ,class: 'text-dark'  %></td></tr>
      <% end %>
    </tbody>
  </table>
